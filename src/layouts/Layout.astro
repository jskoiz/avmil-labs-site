---
import '../styles/global.css';

interface Props { title?: string; description?: string; }
const {
  title = 'AVMIL LABS - Digital Media Company in Honolulu, Hawaii',
  description = "Digital marketing, web development, and software development — built for today's fast-paced, tech-forward landscape."
} = Astro.props;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <title>{title}</title>
  </head>
  <body>
    <slot />
  </body>
</html>
